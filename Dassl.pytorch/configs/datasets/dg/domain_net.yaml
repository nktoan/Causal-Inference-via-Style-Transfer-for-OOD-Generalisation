INPUT:
  SIZE: (224, 224)
  TRANSFORMS: ["random_flip", "random_translation", "colorjitter", "randomgrayscale"]

DATASET:
  NAME: "DomainNet"

MODEL:
  BACKBONE:
    NAME: "resnet50"
    PRETRAINED: True